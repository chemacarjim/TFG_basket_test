server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tfgdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: ${JWT_SECRET:dev-secret-please-change-32bytes-min-length-2025-OK}
    expiration-minutes: 240
  cloudinary:
    cloud-name: dlijkbqr9
    api-key: 829682312575565
    api-secret: 2fLZ6bLyJJGut1Fvn0uFNdAZ5KE

